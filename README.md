# AI Knowledge Inbox

A minimal single-user app to save notes/URLs and ask questions over them via a simple RAG pipeline.

## Status

Work in progress, built incrementally with a commit per milestone.

- [ ] Repo scaffold (FastAPI backend, Vite + React + TS + Tailwind + shadcn frontend)
- [ ] Data model
- [ ] Ingestion (`POST /ingest`)
- [ ] Chunking + embeddings
- [ ] Query (`POST /query`)
- [ ] Frontend UI
- [ ] Logging, error handling, README write-up (tradeoffs, scaling)

## Setup

_To be filled in as each piece is built._

## Design decisions and tradeoffs

_To be filled in as the app is built._
